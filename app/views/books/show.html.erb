<h1>User info</h1>

<%= form_with model: @user url: update_list_path(@user.id), method: :patch do |f| %>

  <h2>Name</h2>
  <%= f.text_field :title %>
  
<h2>Image</h2>
<% if @user.image.attached? %>
  <%= image_tag @user.image %>
<% else %>
  <%= image_tag 'no_image' %>
<% end %>

  <h3>Introduction</h3>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update User' %>
  
<% end %>

<h1>New book</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :Title %>
  <h4>Opinion</h4>
  <%= f.text_area :Opinion %>
   <p><%= link_to 'Create Book', edit_book_path(book.id) %></p>
<% end %>


<h1>Book detail</h1>

<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>

<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <%= image_tag user.get_image %>
  <% end %>
  <p><%= image_tag book.user.get_profile_image(100,100) %></p>
     <p>Name<%= link_to 'book.user.name', book_path(user.id) %></p>
        <p>Title<%= link_to 'book.title', books_path(user.id) %></p>
          <p>Opinion<%= @book.opinion %></p>
           <p><%= link_to 'Edit', edit_book_path(book.id) %></p>
            <p><%= link_to "Destroy", destroy_book_path(@book.id), method: :delete, "data-confirm" => 'Are you sure？' %></p>
</div>
<% end %>