<h1>New book</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :Title %>
  <h4>Opinion</h4>
  <%= f.text_area :Opinion %>
   <p><%= link_to 'Create Book', edit_book_path(book.id) %></p>
<% end %>