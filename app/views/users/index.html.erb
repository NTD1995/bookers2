<%= render 'books/book', books: @books %>


<h1>Users</h1>

<% @users.each do |user| %>
  <div>
    <%= image_tag user.get_image(100,100) %>
    <p>image</p>
    <p>name<%= user.name %></p>
    <p><%= link_to "show", book_path(user.id) %></p>
    
  </div>
<% end %>